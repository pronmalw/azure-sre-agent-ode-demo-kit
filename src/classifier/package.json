{
  "name": "@sre-demo/classifier",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "test": "jest --config jest.config.js --passWithNoTests"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  }
}
